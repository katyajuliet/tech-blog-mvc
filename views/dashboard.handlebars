<section class="form-entry">
    <h3>New Mighty Tech Post</h3>

  <form class="new-post-form" enctype="multipart/form-data">
    <div>
      <label for="post-title">Title</label>
      <input type="text" id="post-title" name="post-title" />
    </div>
    <div>
      <label for="post-text">Body</label>
      <input type="text" id="post-text" name="post-text" />
    </div>
    <button type="submit" class="btn">Create</button>
  </form>
  </form>
</section>

{{#if posts.length}}
    <section>
        <h2>Your Mighty Posts</h2>
        <div>
            {{#each posts as |post|}}
                <div class="edit_post_section">
                  {{> slice post edit =1 loggedIn = ../loggedIn}}
                  
                </div>
            {{/each}}
        </div>
    </section>
{{/if}}

<script src="/javascript/add-post.js"></script>
